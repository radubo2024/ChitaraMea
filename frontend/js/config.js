/**
 * Application Configuration
 * Update API_BASE_URL to match your cPanel hosting URL
 */
const CONFIG = {
    // Change this to your actual backend API URL when deployed
    API_BASE_URL: 'backend/api',
    APP_NAME: 'Acorduri.ro',
    ITEMS_PER_PAGE: 20,
    
    // Use local data mode (no MySQL needed for demo)
    USE_LOCAL_DATA: true
};
